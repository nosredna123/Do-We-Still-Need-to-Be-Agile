{
  "name": "extensaoiii",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace frontend",
    "dev:backend": "cd backend && uv run uvicorn main:app --host 127.0.0.1 --port 8000 --reload",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace frontend"
  },
  "dependencies": {
    "mammoth": "^1.12.0"
  }
}
