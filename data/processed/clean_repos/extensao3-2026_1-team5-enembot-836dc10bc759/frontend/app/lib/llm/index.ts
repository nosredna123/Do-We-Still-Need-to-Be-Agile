/**
 * IA do produto vem somente da API FastAPI (PUT /chat/prompt/...).
 * Não use mock — use `promptAI` e `randomQuestion` em `lib/backendApi.ts`.
 */
export type { CommandActionId, HighlightAction } from './llmClient';
